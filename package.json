{
  "name": "chat-gpt-clone",
  "version": "1.0.0",
  "main": "backend/index.js",
  "type": "module",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "dev": "concurrently \"NODE_ENV=development npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "npm install && npm install --prefix frontend && npm run build --prefix frontend",
    "start": "NODE_ENV=production node backend/index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "A full-stack ChatGPT clone with Express backend and React frontend",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
